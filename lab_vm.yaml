---
  - name: Create a JIRA issue
    uri:
    url: curl -X  GET 'https://vcsa1.home.local/rest/vcenter/vm'
    method: POST
    body: "{{ lookup('file','issue.json') }}"
    force_basic_auth: yes
    body_format: json
